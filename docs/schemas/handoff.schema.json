{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TaskHandoff",
  "type": "object",
  "required": ["taskId", "state", "baseCommit", "headCommit", "evidencePath", "completed", "remaining", "knownRisks", "nextAction"],
  "properties": {
    "taskId": {"type": "string"},
    "state": {"enum": ["READY", "IN_PROGRESS", "BLOCKED", "IN_REVIEW", "ACCEPTED", "REJECTED"]},
    "baseCommit": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
    "headCommit": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
    "evidencePath": {"type": "string"},
    "deliveryTiming": {"$ref": "#/$defs/deliveryTiming"},
    "reviewers": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "kind", "verdict", "reviewedCommit", "evidencePath"],
        "properties": {
          "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]*$"},
          "kind": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]*$"},
          "verdict": {"enum": ["PASS", "FAIL", "TIMEOUT", "UNKNOWN"]},
          "reviewedCommit": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
          "evidencePath": {"type": "string", "minLength": 1},
          "reason": {"type": "string", "minLength": 1},
          "candidateTree": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
          "budget": {"$ref": "#/$defs/nonPassBudget"},
          "interruption": {"$ref": "#/$defs/interruptionObservation"}
        },
        "allOf": [
          {
            "if": {"properties": {"verdict": {"enum": ["TIMEOUT", "UNKNOWN"]}}, "required": ["verdict"]},
            "then": {"required": ["reason", "candidateTree", "budget", "interruption"]}
          }
        ]
      }
    },
    "completed": {"type": "array", "items": {"type": "string"}},
    "remaining": {"type": "array", "items": {"type": "string"}},
    "knownRisks": {"type": "array", "items": {"type": "string"}},
    "nextAction": {"type": "string", "minLength": 1}
  },
  "$defs": {
    "nonPassBudget": {
      "type": "object",
      "required": ["maximumMinutes", "elapsedSeconds", "hardLimitReached"],
      "additionalProperties": false,
      "properties": {
        "maximumMinutes": {"type": "integer", "minimum": 1},
        "elapsedSeconds": {"type": "number", "minimum": 0},
        "hardLimitReached": {"type": "boolean"}
      }
    },
    "interruptionObservation": {
      "type": "object",
      "required": ["terminalOutputReceived", "observedStatus", "action"],
      "additionalProperties": false,
      "properties": {
        "terminalOutputReceived": {"type": "boolean"},
        "observedStatus": {"type": "string", "minLength": 1},
        "action": {"type": "string", "minLength": 1}
      }
    },
    "deliveryTiming": {
      "type": "object",
      "required": ["overallElapsed", "intakeActivation", "candidateExecution"],
      "additionalProperties": false,
      "properties": {
        "overallElapsed": {
          "type": "object",
          "required": ["anchor", "anchorCommit", "startedAt", "endedAt", "elapsedSeconds", "reanchored"],
          "additionalProperties": false,
          "properties": {
            "anchor": {"const": "DRAFT_COMMIT"},
            "anchorCommit": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
            "startedAt": {"type": "string", "minLength": 1},
            "endedAt": {"type": "string", "minLength": 1},
            "elapsedSeconds": {"type": "number", "minimum": 0},
            "reanchored": {"const": false}
          }
        },
        "intakeActivation": {"$ref": "#/$defs/deliveryPhase"},
        "candidateExecution": {
          "oneOf": [
            {"$ref": "#/$defs/candidateExecutionStarted"},
            {"$ref": "#/$defs/candidateExecutionNotStarted"}
          ]
        }
      }
    },
    "candidateExecutionStarted": {
      "type": "object",
      "required": ["anchor", "anchorCommit", "startedAt", "endedAt", "elapsedSeconds", "outcome", "targetWallMinutes", "hardFuseWallMinutes", "candidateDeadlineMinutes", "closureOnlyOverrunSeconds", "readyDoctorPassAt", "inProgressCommit"],
      "additionalProperties": false,
      "properties": {
        "anchor": {"const": "READY_DOCTOR_PASS_AND_IN_PROGRESS_COMMIT"},
        "anchorCommit": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
        "startedAt": {"type": "string", "minLength": 1},
        "endedAt": {"type": "string", "minLength": 1},
        "elapsedSeconds": {"type": "number", "minimum": 0},
        "outcome": {"enum": ["PASS", "FAIL", "TIMEOUT", "UNKNOWN"]},
        "targetWallMinutes": {"const": 60},
        "hardFuseWallMinutes": {"const": 90},
        "candidateDeadlineMinutes": {"const": 45},
        "closureOnlyOverrunSeconds": {"type": "number", "minimum": 0},
        "readyDoctorPassAt": {"type": "string", "minLength": 1},
        "inProgressCommit": {"type": "string", "pattern": "^[0-9a-f]{40}$"}
      }
    },
    "candidateExecutionNotStarted": {
      "type": "object",
      "required": ["anchor", "anchorCommit", "startedAt", "endedAt", "elapsedSeconds", "outcome", "targetWallMinutes", "hardFuseWallMinutes", "candidateDeadlineMinutes", "closureOnlyOverrunSeconds", "readyDoctorPassAt", "inProgressCommit", "reason", "reanchored"],
      "additionalProperties": false,
      "properties": {
        "anchor": {"const": "NOT_STARTED_READY_DOCTOR_NON_PASS"},
        "anchorCommit": {"type": "null"},
        "startedAt": {"type": "null"},
        "endedAt": {"type": "null"},
        "elapsedSeconds": {"const": 0},
        "outcome": {"const": "NOT_STARTED"},
        "targetWallMinutes": {"const": 60},
        "hardFuseWallMinutes": {"const": 90},
        "candidateDeadlineMinutes": {"const": 45},
        "closureOnlyOverrunSeconds": {"const": 0},
        "readyDoctorPassAt": {"type": "null"},
        "inProgressCommit": {"type": "null"},
        "reason": {"type": "string", "minLength": 1},
        "reanchored": {"const": false}
      }
    },
    "deliveryPhase": {
      "type": "object",
      "required": ["anchor", "anchorCommit", "startedAt", "endedAt", "elapsedSeconds", "outcome", "targetWallMinutes", "hardFuseWallMinutes", "closureOnlyOverrunSeconds"],
      "properties": {
        "anchor": {"type": "string", "minLength": 1},
        "anchorCommit": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
        "startedAt": {"type": "string", "minLength": 1},
        "endedAt": {"type": "string", "minLength": 1},
        "elapsedSeconds": {"type": "number", "minimum": 0},
        "outcome": {"enum": ["PASS", "FAIL", "TIMEOUT", "UNKNOWN"]},
        "targetWallMinutes": {"type": "integer", "minimum": 1},
        "hardFuseWallMinutes": {"type": "integer", "minimum": 1},
        "candidateDeadlineMinutes": {"type": "integer", "minimum": 1},
        "closureOnlyOverrunSeconds": {"type": "number", "minimum": 0},
        "readyDoctorPassAt": {"type": "string", "minLength": 1},
        "inProgressCommit": {"type": "string", "pattern": "^[0-9a-f]{40}$"}
      }
    }
  }
}
