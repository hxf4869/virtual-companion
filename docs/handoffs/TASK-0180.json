{
  "taskId": "TASK-0180",
  "state": "ACCEPTED",
  "baseCommit": "456e6d7e4802dcdaf198abff01e773649b9f4c7f",
  "headCommit": "0000000000000000000000000000000000000000",
  "evidencePath": "docs/evidence/TASK-0180/evidence-pack.json",
  "completed": [
    "MemoryRecord + MemoryEvidenceRecord（新，platform.persistence）：get_memory 与 list_memory 两个 out 列集并集（relationshipId/deletedAt 可空）；evidence record 映射 V12 list_memory_evidence out 列；createdAt null→Instant.EPOCH fallback 照 0179。",
    "MemoryService（新，platform.persistence）：8 方法包 V11/V12/V13 SD——create（RelationshipService.get 预检 relationship→404、scope Alpha 校验 SESSION/RELATIONSHIP、SESSION 必带 conversationId、evidence text[] 经 ps.getConnection().createArrayOf、创建后 get_memory 回读 PENDING_CONFIRMATION）/list（includeDeleted 透传，foreign→空数组）/get/update（get 预检+可编辑状态 {PENDING_CONFIRMATION,ACCEPTED}，死端→404）/delete（get 预读→删除→200 删除前快照；已软删→404 Owner 拍板）/confirm/reject（get 预检+PENDING_CONFIRMATION，非 pending→404）/listEvidence；统一翻译：BadSqlGrammarException 上抛保 503，业务 RAISE→Optional.empty→404（memory 端点 OpenAPI 把状态冲突统一映射 NOT_FOUND_OR_FORBIDDEN，与 cancel 的 400 不同），非法输入→IllegalArgumentException→400。",
    "MemoryController（新，runtime.memory.web）：8 端点严格对齐 OpenAPI（POST candidates/GET list/GET/PATCH/DELETE /memories/{id}/POST confirm/REJECT/GET evidence）；@AuthenticationPrincipal(expression=accountId)；parseId 正数校验；includeDeleted String 手解仅 true/false（其余→400，避免 MethodArgumentTypeMismatchException→500）；DTO MemoryResponse/MemoryEvidenceResponse/MemoryCandidateCreateRequest(@NotBlank)/MemoryUpdateRequest(@NotBlank)；empty→ResourceNotFoundException→404；list/evidence 无 404 分支（foreign→200 空数组）。",
    "AuthDataSourceConfig + MemoryService bean（注入 authJdbcTemplate + relationshipService）；Controller 组件扫描注册（@ConditionalOnProperty auth.datasource-enabled=true，与 TASK-0178/0179 同模式）。",
    "测试：MemoryServiceTest 28（persistence，mock JdbcTemplate+RelationshipService，含 SD 不调用 verify、RAISE→empty、BadSqlGrammar 上抛、删除前快照）+ MemoryControllerTest 23（runtime，standalone MockMvc+Principal resolver）；RuntimeModuleStructureTest Modulith PASS。",
    "不新增 DB migration、不改 modelruntime/safety/adapters/specs/auth/conversation/generation/relationship/web/message/cancel 包（只消费 V11/V12/V13 SD 公共函数）→ 不触发 C3/C4；卡片定 C2。"
  ],
  "remaining": [
    "C4 snapshot SD 函数 V26 create_authorization_snapshots + AuthorizationSnapshotProvider JDBC 实现 + loopback adapter e2e（激活 external 运行期路径）。",
    "realtime SSE 推送（SseEmitter + V8 resume_stream）；ID hashid 编码。"
  ],
  "knownRisks": [
    "authorizationCommit 为占位符字符串（精简长线流残留，历史不可改写；doctor ACCEPTED 态不校验该格式），与 TASK-0173..0179 同属已知残留。",
    "delete 已软删（owned 但 deleted_at 非空）→404 NOT_FOUND_OR_FORBIDDEN 与 OpenAPI prose「duplicate delete 返回 success」分歧——Owner 2026-08-12 计划审批拍板：所有 SD 读取路径排除软删行（墓碑），无法构造 OpenAPI 必填字段响应体；SD 层幂等（vc.delete_memory 对 owned 已删返回 TRUE）由 DB test 36 承载；HTTP 层与其他端点一致将软删视为 absent。",
    "Memory 响应不含 deletedAt（OpenAPI Memory schema 无该字段）；includeDeleted=true 返回的软删行以原 status 呈现（前端默认 includeDeleted=false）。",
    "update/confirm/reject 的并发竞态 RAISE（预检通过后状态迁移）→404：OpenAPI 明确把死端/非 pending 映射 NOT_FOUND_OR_FORBIDDEN（与 cancel 的 400 不同，逐端点核对结论）；非法输入仍→400 INVALID_REQUEST。",
    "DTO id 仍为 long（OpenAPI 各 id 是 string；long 序列化为数字字符串，与 TASK-0174/0178/0179 一致）；hashid 编码 deferred。",
    "canonical precheck / doctor --summary / 完整 Harness unittest discover / 根级 Maven verify deferred per Owner 2026-08-12 static-gates-only。"
  ],
  "nextAction": "TASK-0180（memory HTTP 端点，C2）闭环 ACCEPTED：实现 OpenAPI 已定义但 Java HTTP 层缺失的 8 个端点——POST /api/v1/relationships/{relationshipId}/memories/candidates（createMemoryCandidate，foreign relationship→404） +GET /api/v1/relationships/{relationshipId}/memories（listMemories，includeDeleted 手解仅 true/false， foreign→200 空数组）+GET /api/v1/memories/{memoryId}（getMemory，foreign/absent/deleted→404）+PATCH /api/v1/memories/{memoryId}（updateMemory，死端状态→404）+DELETE /api/v1/memories/{memoryId}（deleteMemory， alive→200 删除前快照，已软删→404 Owner 拍板）+POST /api/v1/memories/{memoryId}/confirm（confirmMemoryCandidate， 非 pending→404）+POST /api/v1/memories/{memoryId}/reject（rejectMemoryCandidate，非 pending→404）+GET /api/v1/memories/{memoryId}/evidence（listMemoryEvidence，foreign/absent→200 空数组）；复用 V11/V12/V13 SD 函数（均已 GRANT vc_api，vc_api 无 memory_item 直写）；新 MemoryService+MemoryRecord+MemoryEvidenceRecord （platform.persistence 包：create 经 RelationshipService.get 预检、evidence text[] 经 createArrayOf、 BadSqlGrammarException 上抛保 503、业务 RAISE→404——memory 端点 OpenAPI 把状态冲突统一映射 NOT_FOUND_OR_FORBIDDEN，与 cancel 的 400 不同）+MemoryController（runtime.memory.web，自带 MemoryResponse/MemoryEvidenceResponse/MemoryCandidateCreateRequest/MemoryUpdateRequest DTO）+AuthDataSourceConfig 1 bean；复用 runtime.web 错误层（404 NOT_FOUND_OR_FORBIDDEN/400 INVALID_REQUEST）；MemoryServiceTest 28+MemoryControllerTest 23 场景；run-rls 67/67+mvn runtime 304/0/0（persistence 94/0/0，Modulith PASS）+diff exit0 全绿。不新增 migration、不改 modelruntime/safety/adapters/specs/auth/conversation/generation/relationship/ web/message/cancel 包→不触发 C3/C4。下一步：C4 snapshot SD 函数 V26 create_authorization_snapshots+ AuthorizationSnapshotProvider JDBC 实现+loopback adapter e2e（激活 external 运行期路径）按序。"
}
