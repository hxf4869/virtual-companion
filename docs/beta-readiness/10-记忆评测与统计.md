# 10 · 记忆评测与统计（MEM-EVAL，§26.4 / R45）

**状态：工具与自动化样本已就绪；人工标注由 Owner 投入。**

## 1. 自动化部分（已实现，可重复执行）

| 指标 | 阈值 | 工具 |
|---|---|---|
| 删除复活率 | =0（≥100 回归样本） | `bash infra/db/measure/run-measure.sh 80_mem_delete_resurrection`（每样本：确认→删除墓碑→结构化+语义双路召回，复活即 FAIL） |
| 跨关系错误召回 | =0（≥1,000 探测） | `... 81_cross_relationship_recall`（同 owner 双关系隔离事实，B 关系查 A 事实 1,000 次） |
| 敏感自动保存率 | =0（≥200 敏感样本） | `MeasureSensitiveAutoSaveTest`（runtime 测试套件内；敏感词嵌入白名单句式，规则必须整体拒绝） |

## 2. 人工标注部分（Owner）

```bash
# 1) 生成标注工作集（确定性种子,可重复）
python3 scripts/measure/gen_mem_eval_samples.py workset/
#   candidates.tsv ≥200 条候选（Owner 逐条判 grounded/not_grounded）
#   recall_scenarios.tsv ≥200 个召回场景（Owner 逐条判 hit/miss/wrong）

# 2) 标注完成后计算指标
python3 scripts/measure/mem_eval_stats.py \
    --candidates workset/candidates.tsv \
    --recall workset/recall_scenarios.tsv \
    --resurrection <phase80 输出> --cross-leak <phase81 输出>
```

阈值：Memory Precision ≥90%（n≥200）、Recall Precision ≥95%（n≥200）、
删除复活率 =0、敏感自动保存率 =0、跨关系错误召回 =0。

## 3. Owner 待填清单

- `[标注完成日期与人力投入]`
- `[未达线时的处置]`：调阈值/调规则/换提取策略（写入 ADR）
