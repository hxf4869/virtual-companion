# 产品定义：Virtual Companion

> 状态：2026-09-01 前端重做基线
> 本文件定义产品目的、范围和行为边界；页面结构见
> `docs/product/frontend-redesign.md`，视觉与组件约束见 `frontend/DESIGN.md`。
> 旧的 Technical Alpha、Owner-only 和历史前端规划只作为实现背景，不再约束新前端。

## 一句话产品

让用户随时回到同一个熟悉的 AI 陪伴者身边，继续一段自然、连续、无需学习专业术语的文字对话。

产品不是模型控制台、调试工具或功能目录。用户首先要知道“现在能做什么”和“下一步点哪里”，
不需要理解模型、路由、Token、记忆生命周期或内部状态。

## 用户与使用场景

- 面向使用手机 H5 的普通成年用户，后续复用同一消费者端工程生成微信小程序。
- 用户在日常空闲、情绪低落或想说话时打开产品，继续上次对话或开始新的对话。
- 管理员通过独立的 H5 后台处理注册审核、账号、模型与路由、运行状态。
- 正式名称与 Logo 尚未确定；`Virtual Companion / 虚拟陪伴` 是工作名称。

## 产品承诺

1. **对话优先**：核心路径始终是登录、回到首页、继续对话。
2. **不用猜**：标题、按钮和状态使用普通简体中文；当前主动作一眼可见。
3. **连续但不过度解释**：产品可以延续上下文，但不把内部记忆机制暴露给用户。
4. **如实反馈**：等待、失败、无内容和不可用都说明发生了什么，以及用户能做什么。
5. **边界安静可见**：保留“AI 陪伴者 · 非真人”，不冒充真人，也不让安全提示淹没主要任务。
6. **没有假功能**：未开放的能力不做入口、占位卡、假数据或“敬请期待”。

## 首次上线范围

### 消费者端

- 账号与密码登录；账号可以是用户名或邮箱。
- 登录后的身份验证器验证码验证。
- 首页：继续上次对话、查看最近对话、进入全部会话。
- 文字聊天：查看消息、发送消息、处理发送失败。
- 我的：账号、安全和必要设置。
- 底部导航固定为“首页 / 聊天 / 我的”。

### 注册与准入

- 上线前关闭公开注册；关闭必须由服务端执行，前端只显示“注册暂未开放”。
- 以后开放时使用邮箱、密码和邮箱验证码，不使用短信。
- 新注册账号先进入审核，审核通过前不能聊天。
- 审核通过后的首次登录需要绑定 Authenticator 类身份验证器并保存恢复码。
- 完成身份验证器设置后，服务端自动创建唯一默认陪伴关系；不增加创建陪伴者或选择人格步骤。
- 后续登录默认是“账号 + 密码 → 6 位身份验证器验证码”；用户可以在完成验证码后选择信任当前设备 90 天，
  有效期内仍需输入账号和密码，但可以跳过验证码。
- 身份验证器绑定的是账号安全方式，不称为“绑定设备”；“可信设备”只指用户明确选择的免验证码设备，
  可以在账号安全中查看和撤销。
- 未来公开注册的成年准入与必要同意方式后期单独决定；决定完成前继续关闭公开注册。

### 管理后台

- 注册审核。
- 账号管理，包括身份验证器状态与人工重置入口。
- 模型与路由配置。
- 运行状态。

后台只构建 H5，不进入微信小程序包。

## 不在本轮范围

- 公开注册、短信验证码、支付、订阅、语音、图片、多陪伴者和社交功能。
- 在首页展示未来功能、运营假指标或没有真实数据源的推荐内容。
- 向消费者展示模型名称、Token、Prompt、路由、Provider、内部 ID、调试信息或原始错误。
- 因为旧代码中存在页面就自动保留旧入口。旧的记忆、同意、提醒、技术状态等页面必须重新获得
  明确的产品用途后，才能进入新导航。
- 管理后台进入微信小程序。

## 信息架构

### 未登录

登录 → 验证登录 → 首页。

注册关闭时，登录页只说明“注册暂未开放”。未来开放后的流程为：

注册 → 邮箱验证 → 等待审核 → 首次登录绑定身份验证器 → 首页。

### 已登录消费者

- **首页**：回到关系、继续上次对话、浏览最近对话。
- **聊天**：进行当前对话；“全部会话”是聊天域内的列表页，不增加第四个底部导航。
- **我的**：账号资料、安全方式和低频设置。

### 管理员

注册审核 / 账号 / 模型与路由 / 运行状态。

## 成功标准

- 新用户无需帮助即可完成登录和验证码验证。
- 已登录用户打开首页后，一个主动作内可以继续最近对话。
- 用户能找到全部会话，但首页不会变成会话管理后台。
- 聊天页的输入、发送、失败重试和返回路径清楚，底部输入栏与导航互不遮挡。
- 审核中的账号不会看到可发送消息的聊天界面。
- 消费者页面没有必须查询才能理解的专业术语。
- H5 与未来微信小程序保持相同的消费者信息架构和产品组件语言。

## 后续单独决定

- 正式品牌名、Logo 与插画资产。
- 公开注册的开放时间、驳回通知与再次申请规则。
- 公开注册的成年准入与必要同意方式。
- 身份验证器丢失后的人工核验细则。
- 首页未来新增模块；只有真实能力和真实数据准备好后才设计入口。
