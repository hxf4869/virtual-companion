schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0185
lastAcceptedHandoff: docs/handoffs/TASK-0185.json
lastTerminalTask: TASK-0185
lastTerminalHandoff: docs/handoffs/TASK-0185.json
nextAction: >-
    TASK-0185（H5 realtime transport 对齐 0184 resume endpoint 契约）闭环
    ACCEPTED：realtime-transport.ts（mint ticket POST /tickets→GET
    /streams/{id}?ticket query+Last-Event-ID header+mapFrames 5
    disposition；存在性隐藏 NOT_FOUND_OR_FORBIDDEN；5xx throw；abort 透传）+
    sse-parser 扩展 event: 行解析（控制事件 stream.gap/reset/denied 不再丢弃）+ chat.vue
    移除内联 transport 用新模块 + 19 项 transport spec + 3 项 sse-parser event
    spec；frontend vitest 170/0/0 + type-check exit0 + diff
    exit0。下一步：TASK-0025 Chat/Generation/History API（发送幂等+sessionId
    真实来源+history 分页）纵切按序（开卡时分配下一永久 ID）；policy 测试卡住独立排查随后。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-13'
