schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0174
lastAcceptedHandoff: docs/handoffs/TASK-0174.json
lastTerminalTask: TASK-0174
lastTerminalHandoff: docs/handoffs/TASK-0174.json
nextAction: TASK-0174（Generation HTTP 纵切，C4）闭环 ACCEPTED：V25 create_conversation/enqueue_work_item/promote_generation 三 SD 函数 + conversation_id_seq/work_item_id_seq 授 vc_api + 3 persistence service（WorkItemEnqueue/ConversationCreate/GenerationState）+ 2 HTTP 端点（POST /conversations + POST intake + GET snapshot，@AuthenticationPrincipal expression 避免 Modulith 跨模块依赖）+ GenerationWorkItemHandler 替换 LoggingWorkItemHandler（promote IN_PROGRESS→terminalize FAILED_FINAL 降级）+ AuthDataSourceConfig bean 装配 + test 65（create/enqueue/promote 正负）+ handler unit 4 场景 + SchemaReadiness 24→25；run-rls 65/65 + runtime mvn 254/0/0 + diff exit0。真实模型调用（LiveModelInvoker.invoke 构建 LiveInvocationRequest）+ SSE + listMessages/cancel/relationship 端点留后续卡。下一步：开后续卡——LiveModelInvoker 成功路径接线（routing inputs 设计）解锁 generation COMPLETED；其余端点按序开卡。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
