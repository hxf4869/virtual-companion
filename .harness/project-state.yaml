schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0158
activeTaskCard: docs/tasks/TASK-0158-risk-09-search-path-hardening.md
lastAcceptedTask: TASK-0161
lastAcceptedHandoff: docs/handoffs/TASK-0161.json
lastTerminalTask: TASK-0161
lastTerminalHandoff: docs/handoffs/TASK-0161.json
nextAction: '执行 TASK-0158（RISK-09 search_path 收紧）：新增 V18 migration（ALTER FUNCTION 把 schema vc 全部 SECURITY DEFINER 函数 search_path 改为 vc,pg_catalog + REVOKE CREATE ON SCHEMA public FROM PUBLIC，不改 V1-V17 历史）+ 57 号 cross-tenant 负测（G1 SD proconfig=vc,pg_catalog 无 public 计数≥37 / G2 runtime 角色与 PUBLIC 对 public 无 CREATE 且 vc_api 建 public 函数被拒 / G3 public 同名函数 shadow 不可劫持 SD 解析）；C4 database-migration + humanApproval(scope: database-migration) + 独立 Reviewer；DB 变更跑 run-rls-tests.sh（OrbStack pgvector:0.8.5-pg18 digest-pinned，56→57 测试）+ canonical precheck 8/8 + git diff --check + R1 静态（Owner 2026-08-12 static-gates-only，完整 Harness unittest 留统一审计）。终态单父 [skip ci] ACCEPTED + push + 远端 0/0/clean。'
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
