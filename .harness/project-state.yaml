schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0109
activeTaskCard: docs/tasks/TASK-0109-model-request-size-limits.md
lastAcceptedTask: TASK-0108
lastAcceptedHandoff: docs/handoffs/TASK-0108.json
lastTerminalTask: TASK-0108
lastTerminalHandoff: docs/handoffs/TASK-0108.json
nextAction: >-
  TASK-0109 READY（工作包 12
  第三张 P2-06 尺寸/成本上限）：新增
  SizeLimits 集中上限（Owner 确认方案 A：消息数 ≤64、
  单条 ≤64 KiB、schema ≤16 KiB、单流事件 ≤1 MiB、
  累计输出 ≤1 MiB、OpenAI max_tokens ≤8192）；
  domain 层 ProtocolMessage/LiveInvocationRequest/
  ModelProtocolRequest 构造校验 + adapter 层 Codec
  max_tokens/schema、Session 单事件/累计输出上限
  （超限失败关闭）+ LiveModelInvoker 输出兜底与测试；
  完成后条件风险 6/7（db/migration C4 +
  database-migration + humanApproval）继续等待；生产接线（controller/dispatcher，TASK-0093）不受本卡影响；工作包
  6（P1-04+P1-05+P2-29+条件风险 9 租户与 DB
  权限架构）继续等待 Owner 决策可信数据库
  principal/连接池绑定模型与 migration
  principal 部署拓扑；Actions
  配额耗尽下的远端复核延后至配额恢复。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-09'
