schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0165
activeTaskCard: docs/tasks/TASK-0165-quota-nonneg-check-and-release-idempotency.md
lastAcceptedTask: TASK-0164
lastAcceptedHandoff: docs/handoffs/TASK-0164.json
lastTerminalTask: TASK-0164
lastTerminalHandoff: docs/handoffs/TASK-0164.json
nextAction: 'TASK-0165（§5.1.4 quota 非负 CHECK + release 幂等）READY/IN_PROGRESS：纯 DB V21（generation_usage.{input_tokens,output_tokens,actual_cost} + quota_ledger_entry.quota_amount 四条 CHECK(>=0) + partial unique index quota_ledger_release_one_per_generation ON (owner_user_id,generation_id) WHERE kind=RELEASE + CREATE OR REPLACE record_quota_release 加幂等守卫签名不变保留 V17 trusted-context）+ 新 test 60 integration_test；C4 database-migration + static-gates-only 验证（run-rls-tests.sh 59→60 + canonical precheck 8/8 + diff，完整 Harness unittest 留统一审计）。完成后回写终态 nextAction。'
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
