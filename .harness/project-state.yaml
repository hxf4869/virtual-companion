schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0154
activeTaskCard: docs/tasks/TASK-0154-sd-owner-param-trusted-assertion.md
lastAcceptedTask: TASK-0153
lastAcceptedHandoff: docs/handoffs/TASK-0153.json
lastTerminalTask: TASK-0153
lastTerminalHandoff: docs/handoffs/TASK-0153.json
nextAction: 完成 TASK-0154（P1-04 SD 函数 owner 参数服务端可信收敛）：V17 所有 34 个含 p_owner_user_id 的 SECURITY DEFINER 函数加 current_owner_id 一致性断言（强模式 fail-closed：IS DISTINCT FROM 含 NULL 拒绝，移除函数内部 set_config owner，保留参数 Java 调用点不改签名）+ 9 现有测试重构加 SET LOCAL/set_config vc.owner_user_id 前置 + 2 新 SQL 负测（caller-mismatch/missing-context）；之后按 Owner 授权处理 P1-11（TASK-0155）、P2-03（TASK-0156）、P2-27（TASK-0157）、RISK-09（TASK-0158）；全部修复后对最新 origin/main 做独立全项目复审。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-11'
