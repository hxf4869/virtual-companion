schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0160
lastAcceptedHandoff: docs/handoffs/TASK-0160.json
lastTerminalTask: TASK-0157
lastTerminalHandoff: docs/handoffs/TASK-0157.json
nextAction: '建立 TASK-0161 replacement（TASK-0157 REJECTED TIMEOUT）：以 TASK-0157 REJECTED terminal 为 Base，候选 872311a 实现（4 文件 ci-execution-policy.yaml/doctor.py/test_harness.py/ci.yml 改动）已在 Base 中不重复，writeAllowlist 仅治理制品；deliveryBudgets.hardFuseWallMinutes=180（覆盖完整 Harness unittest × 2 串行 + canonical + R1 + closure，避免并行 IO 争用 hung 重演）；只跑完整 Harness unittest（串行）+ canonical precheck + git diff --check + R1 独立复核 + 终态 ACCEPTED 闭环。C4 harness-change + humanApproval(scope: harness-change)；之后按 Owner 授权处理 RISK-09（TASK-0158）；全部修复后对最新 origin/main 做独立全项目复审。'
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
