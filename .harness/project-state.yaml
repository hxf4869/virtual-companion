schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0122
lastAcceptedHandoff: docs/handoffs/TASK-0122.json
lastTerminalTask: TASK-0122
lastTerminalHandoff: docs/handoffs/TASK-0122.json
nextAction: >-
  通过 task-intake 创建 TASK-0123：统一 Auth request-target 的 parsed/canonical 边界，修复 admin/accounts percent-encoded 或 matrix MVC 等价路径绕过 16384-byte body fence，并将到达 filter 的 malformed percent request-target 固定为 400 INVALID_REQUEST 且 body zero-read；同时把 AuthInputLimits 对 username、password、displayName、role 和 token 的 UTF-8 检查改为严格 REPORT 语义，防止孤立 surrogate 进入 JDBC 或响应。保持 route 权限、JWT、CSRF、cookie 和数据库状态机语义不变；完成独立 Reviewer、正式门禁、Evidence/Handoff、单父终态提交、推送和远端 0/0 复核。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-09'
