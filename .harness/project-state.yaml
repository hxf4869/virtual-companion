schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0181
lastAcceptedHandoff: docs/handoffs/TASK-0181.json
lastTerminalTask: TASK-0181
lastTerminalHandoff: docs/handoffs/TASK-0181.json
nextAction: >-
    TASK-0181（C4 snapshot 卡）闭环 ACCEPTED：激活 external 运行期路径——V26
    create_authorization_snapshots SECURITY DEFINER 函数（V17 trusted-owner 断言 +
    generation 存在性校验 + purpose 白名单 + 双 ACTIVE 行 + REVOKE PUBLIC/GRANT vc_api）+
    JdbcAuthorizationSnapshotProvider （platform.persistence：providerId 按
    DeterministicRouter 候选规则从 registry 推导、SD 铸造双快照后 mirror 进
    InMemoryAuthorizationSnapshotStore 供 ExecutionAuthorizationGuard
    读取、BadSqlGrammarException 上抛保 worker fail closed）+ AuthDataSourceConfig
    bean（方法级 model-providers.enabled=true 条件）+ external-attempt
    配置扩展（region/contract-ref/purpose/data-categories 默认
    us/alpha-standard/COMPANION_CHAT/[MESSAGE_TEXT]）+
    LoopbackModelProtocolAdapter（runtime/loopback，protocol=FAKE、只接受
    ExternalAttemptBinding、确定性事件流 usage 42/58）+ ApprovedModelProviderProvisioner
    FAKE 分支；run-rls 69/69（新增 68 号：vc_api 经 SD 创建双快照走完整 external 链 COMPLETED + 未知
    generation/跨 owner/空 provider RAISE）+ mvn runtime 311/0/0 （persistence
    102/0/0，新增 JdbcAuthorizationSnapshotProviderTest 8 + loopback 7 场景）+ diff
    exit0 全绿；SchemaReadinessHealthIndicatorTest 随 V26 更新为 26。下一步：realtime SSE
    推送（SseEmitter + V8 resume_stream）按序；ID hashid 编码随后。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
