schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0167
activeTaskCard: docs/tasks/TASK-0167-quota-ledger-release-reservation-idempotency.md
lastAcceptedTask: TASK-0166
lastAcceptedHandoff: docs/handoffs/TASK-0166.json
lastTerminalTask: TASK-0166
lastTerminalHandoff: docs/handoffs/TASK-0166.json
nextAction: 'TASK-0167（§5.1.4 内存 QuotaLedger.release 收 reservationId 做 reservation 级幂等）进行中：C3 model-routing-change Java 卡，修复 release 收 QuotaReservation + releasedReservations 原子幂等去重 + reserve 加 sequence 前缀使单 ledger 内 reservationId 唯一（跨 fresh ledger 同 event 仍 deterministic），GenerationRecovery 调用方同步传 reservation，QuotaLedgerTest 加幂等负测；完成后继续推进剩余 §5.1 候选（§5.1.2 worker fence 依赖 P1-04 OWNER_GATE；ProviderAttemptAudit 投影属 P2-12 JDBC 接线；P1-04/05/11、P2-03、P2-22 需 Owner 决策），全部修复后统一全项目复审（串行完整 unittest + canonical + 根级 Maven verify）。'
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
