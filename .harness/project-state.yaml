schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0170
activeTaskCard: docs/tasks/TASK-0170-p2-03-audit-event-retention-purge.md
lastAcceptedTask: TASK-0169
lastAcceptedHandoff: docs/handoffs/TASK-0169.json
lastTerminalTask: TASK-0169
lastTerminalHandoff: docs/handoffs/TASK-0169.json
nextAction: 'TASK-0170（P2-03 审计事件保留与定时清理，C4 DB）执行中：V22 新增 SECURITY DEFINER purge 函数 identity_auth_event_purge(p_cutoff timestamptz) DELETE occurred_at<cutoff RETURN count + SET search_path=vc,pg_catalog 匹配 V18 基线 + GRANT EXECUTE vc_api/REVOKE PUBLIC；runtime AuthDataSourceConfig 加 @EnableScheduling + IdentityAuthEventPurgeScheduler plain class @Scheduled 每日 03:17 调用（retention 默认 180 天/cron 经 @Value 可配置）+ 单测；infra/db test 62 正测 purge 删旧留新 + 负测 vc_api 无表级 DELETE fail-closed。触 **/db/migration/** C4+database-migration+humanApproval scope:database-migration+independentReview:required。完成后继续逐项解锁剩余 Owner-gate（§5.1.2 worker claim lease/fence 依赖 coordinator、P2-22 nextAction 未注册任务）；全部修复后对最新 origin/main 做独立全项目复审（串行完整 Harness unittest discover + canonical precheck + 根级 Maven verify）。'
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
