schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0186
lastAcceptedHandoff: docs/handoffs/TASK-0186.json
lastTerminalTask: TASK-0186
lastTerminalHandoff: docs/handoffs/TASK-0186.json
nextAction: >-
    TASK-0186（H5 Chat 发送流程 + 历史 API 消费纵切）闭环 ACCEPTED：chat.ts typed API
    client（createConversation/sendGeneration/listMessages/cancelGeneration
    + ChatHttpError 存在性隐藏 + asId string|number 归一）+
    authed-fetch.ts（Bearer+CSRF fetch wrapper 注入 realtime-transport 不改模块）+
    stores/chat.ts send/loadHistory/initConversation（crypto.randomUUID 幂等键
    → sendGeneration → stream → reload history）+ chat.vue 真实发送
    UI（消息输入+发送+历史显示+sessionId UUID+认证 realtime）+ OpenAPI POST
    /api/v1/conversations 端点（补后端已有 drift）+
    CreateConversationRequest/ConversationResponse schema + dist
    重新生成；frontend vitest 196/0/0 + type-check exit0 + openapi
    validate/diff PASS + diff exit0。下一步：开卡时分配下一永久 ID，方向待 Owner
    决策（候选：relationship 选择器 UI / e2e 纵切 / memory 纵切 / ID hashid 编码）。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-13'
