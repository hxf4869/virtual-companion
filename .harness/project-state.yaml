schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0171
activeTaskCard: docs/tasks/TASK-0171-p1-04-worker-half-owner-injection.md
lastAcceptedTask: TASK-0170
lastAcceptedHandoff: docs/handoffs/TASK-0170.json
lastTerminalTask: TASK-0170
lastTerminalHandoff: docs/handoffs/TASK-0170.json
nextAction: 'TASK-0171（P1-04 worker 半边，C4）READY 授权中：WorkItemClaimService 装配进 runtime + worker 路径 OwnerContext.asOwner 服务端注入（Owner 2026-08-12 已定：asOwner 服务端注入、runtime 同进程）+ V23 授权 vc_api EXECUTE claim 家族函数 + test 63。下一步：IN_PROGRESS 实现后最小验证（mvn runtime 定向测试 + run-rls-tests.sh + canonical precheck + diff check）+ R1 语义审查 + 终态闭环；闭环后继续剩余 Owner-gate（§5.1.2 worker claim lease/fence 依赖 coordinator 落地、P2-22 nextAction 未注册任务显式 task-intake 动作策略）；全部修复后对最新 origin/main 做独立全项目复审（串行完整 Harness unittest discover + canonical precheck + 根级 Maven verify，作为 longline 收尾总 PASS）。'
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
