schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0029
activeTaskCard: docs/tasks/TASK-0029-memory-recall-context-tombstone.md
lastAcceptedTask: TASK-0028
lastAcceptedHandoff: docs/handoffs/TASK-0028.json
lastTerminalTask: TASK-0028
lastTerminalHandoff: docs/handoffs/TASK-0028.json
nextAction: TASK-0029 进行中（READY/IN_PROGRESS）。跨会话召回 + Context 注入 + 删除墓碑建在 TASK-0027/0028 V11/V12 之上——V13 前向迁移新增 recall_memory（确定性召回 status=ACCEPTED 且非删除的 owned 记忆；RELATIONSHIP scope 跨 Conversation 召回，SESSION scope 仅在该 conversation_id 时召回；来源分组 + 确定性排序 scope,created_at,id + 预算 LIMIT 钳制 [1,100]）；删除墓碑经 deleted_at IS NULL 结构性排除（无向量/缓存可复活）；跨 owner/跨 relationship/未确认/已删除全空或失败关闭（INV-TENANT-001）。纯 DB 卡（无 OpenAPI/Java/catalog 改动），single protected surface 为 database-migration C4。SQL 测试 37（召回 ACCEPTED-only/budget/scope/隔离）+ 38（墓碑/删除传播/确定性）。完成后 DAG 下一张可晋级卡 TASK-0030（deps 含 TASK-0029）。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-07'
