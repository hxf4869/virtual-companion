schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0172
lastAcceptedHandoff: docs/handoffs/TASK-0172.json
lastTerminalTask: TASK-0172
lastTerminalHandoff: docs/handoffs/TASK-0172.json
nextAction: TASK-0172（P2-22 nextAction 治理，C4）闭环 ACCEPTED：Doctor 区分 advisory 文本与可执行任务引用——project-state.nextAction
  引用的每个 TASK ID 必须已注册（discover_tasks 命中：ledger 执行链/backlog PLANNED/idle DRAFT 例外卡），未注册引用失败关闭，advisory
  文本（如「先通过 task-intake 创建 DRAFT」）放行；doctor.py validate_nextaction_registered_references
  挂载 validate_project_state + test_harness.py 3 测试；canonical 8/8（doctor 827795）+ 定向
  harness 106/0 + diff exit0 + R1 PASS。P2-22 已闭环。下一步：继续逐项解锁剩余 Owner-gate（§5.1.2 worker
  claim lease/fence 依赖 coordinator 落地——Owner 定范围后开卡；其余 OPEN 审计项按序开卡）；全部修复后对最新 origin/main
  做独立全项目复审（串行完整 Harness unittest discover + canonical precheck + 根级 Maven verify，作为
  longline 收尾总 PASS）。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
