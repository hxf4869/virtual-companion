schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0171
lastAcceptedHandoff: docs/handoffs/TASK-0171.json
lastTerminalTask: TASK-0171
lastTerminalHandoff: docs/handoffs/TASK-0171.json
nextAction: 'TASK-0171（P1-04 worker 半边，C4）闭环 ACCEPTED：WorkItemClaimService 装配进 runtime（@Bean ×3）+ worker 路径 OwnerContext.asOwner 服务端注入（Owner 2026-08-12 定：asOwner 注入、runtime 同进程）+ V23 GRANT EXECUTE claim 家族函数 TO vc_api（V17 断言保证仅 server-trusted context 内有效）+ test 63（同事务全链路/无 context RAISE/迟到写零写入）+ WorkItemWorkerTest 5 测试。P1-04 应用侧（TASK-0168）+ worker 侧（本卡）均已闭合。下一步：继续逐项解锁剩余 Owner-gate（§5.1.2 worker claim lease/fence 依赖 coordinator 落地、P2-22 nextAction 未注册任务显式 task-intake 动作策略）；全部修复后对最新 origin/main 做独立全项目复审（串行完整 Harness unittest discover + canonical precheck + 根级 Maven verify，作为 longline 收尾总 PASS）。'
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
