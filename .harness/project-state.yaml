schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0030
activeTaskCard: docs/tasks/TASK-0030-h5-memory-management.md
lastAcceptedTask: TASK-0029
lastAcceptedHandoff: docs/handoffs/TASK-0029.json
lastTerminalTask: TASK-0029
lastTerminalHandoff: docs/handoffs/TASK-0029.json
nextAction: TASK-0030 进行中（READY/IN_PROGRESS）。H5 记忆管理界面建在 TASK-0026 frontend 范式 + TASK-0028 memory API + TASK-0029 recall 之上——新建 api/memory.ts（transport 注入，existence-hidden 映射）、stores/memory.ts（pending/canonical 分离，未确认永不混 canonical；删除失败不乐观伪装成功；来源/状态随 API 真源）、pages/memory/memory.vue（pending 区+canonical 区，确认/拒绝/编辑/删除+来源，失败显错误态）、pages.json 路由、2 vitest spec。frontend/** 未保护路径，single protected surface 无，riskClass C4 policySurfaces AUTHORIZATION 无 split，humanApprovals scope=task-authorization。完成后 DAG 下一张可晋级卡 TASK-0031。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-07'
