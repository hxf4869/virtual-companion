schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0183
lastAcceptedHandoff: docs/handoffs/TASK-0183.json
lastTerminalTask: TASK-0183
lastTerminalHandoff: docs/handoffs/TASK-0183.json
nextAction: >-
    TASK-0183（doctor task-ledger grandfather 派生校验卡）闭环 ACCEPTED：长检查补跑发现
    task-ledger 的 TASK-0177~0182 漏写 state+contractVersion（doctor 30
    errors、unittest 1 FAIL 同源），因 validate_ledger_edge 历史逐字段深比较 append-only
    锁死无法回填 ledger，改 doctor validate_task_ledger_entries 加 grandfather 派生分支（恰好缺
    state+contractVersion 的 3 字段条目从 task card 派生校验 + warn 而非
    error，edge/card/文件存在校验不变）+ test_harness pop 逻辑修复 + 3 个 grandfather
    新测试；doctor 0 errors/6 warnings + BacklogTests 全 PASS + precheck 全 7 绿 + diff
    exit0。下一步：realtime SSE resume 端点（TASK-0184，SseEmitter + V8 resume_stream +
    契约补 resume endpoint，C3 contract-change）按序；policy 测试卡住独立排查随后。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
