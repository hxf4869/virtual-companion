schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0180
lastAcceptedHandoff: docs/handoffs/TASK-0180.json
lastTerminalTask: TASK-0180
lastTerminalHandoff: docs/handoffs/TASK-0180.json
nextAction: >-
  TASK-0180（memory HTTP 端点，C2）闭环 ACCEPTED：实现 OpenAPI 已定义但 Java HTTP 层缺失的 8 个端点——POST
  /api/v1/relationships/{relationshipId}/memories/candidates（createMemoryCandidate，foreign relationship→404）
  +GET /api/v1/relationships/{relationshipId}/memories（listMemories，includeDeleted 手解仅 true/false，
  foreign→200 空数组）+GET /api/v1/memories/{memoryId}（getMemory，foreign/absent/deleted→404）+PATCH
  /api/v1/memories/{memoryId}（updateMemory，死端状态→404）+DELETE /api/v1/memories/{memoryId}（deleteMemory，
  alive→200 删除前快照，已软删→404 Owner 拍板）+POST /api/v1/memories/{memoryId}/confirm（confirmMemoryCandidate，
  非 pending→404）+POST /api/v1/memories/{memoryId}/reject（rejectMemoryCandidate，非 pending→404）+GET
  /api/v1/memories/{memoryId}/evidence（listMemoryEvidence，foreign/absent→200 空数组）；复用 V11/V12/V13 SD
  函数（均已 GRANT vc_api，vc_api 无 memory_item 直写）；新 MemoryService+MemoryRecord+MemoryEvidenceRecord
  （platform.persistence 包：create 经 RelationshipService.get 预检、evidence text[] 经 createArrayOf、
  BadSqlGrammarException 上抛保 503、业务 RAISE→404——memory 端点 OpenAPI 把状态冲突统一映射
  NOT_FOUND_OR_FORBIDDEN，与 cancel 的 400 不同）+MemoryController（runtime.memory.web，自带
  MemoryResponse/MemoryEvidenceResponse/MemoryCandidateCreateRequest/MemoryUpdateRequest DTO）+AuthDataSourceConfig
  1 bean；复用 runtime.web 错误层（404 NOT_FOUND_OR_FORBIDDEN/400 INVALID_REQUEST）；MemoryServiceTest
  28+MemoryControllerTest 23 场景；run-rls 67/67+mvn runtime 304/0/0（persistence 94/0/0，Modulith PASS）+diff
  exit0 全绿。不新增 migration、不改 modelruntime/safety/adapters/specs/auth/conversation/generation/relationship/
  web/message/cancel 包→不触发 C3/C4。下一步：C4 snapshot SD 函数 V26 create_authorization_snapshots+
  AuthorizationSnapshotProvider JDBC 实现+loopback adapter e2e（激活 external 运行期路径）按序。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
