schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0182
lastAcceptedHandoff: docs/handoffs/TASK-0182.json
lastTerminalTask: TASK-0182
lastTerminalHandoff: docs/handoffs/TASK-0182.json
nextAction: >-
    TASK-0182（realtime ticket HTTP 端点卡）闭环 ACCEPTED：把契约（realtime-contract.yaml
    L7）已定义的 POST /api/v1/realtime/tickets 端点同步进 OpenAPI 源（specs/openapi 非保护路径）+
    openapi_tool.py generate 重建 dist
    生成物（api-bundle/snapshot/VirtualCompanionApi.java/typescript/2 个 record，drift
    PASS）+ RealtimeTicketController（runtime/realtime/web：owner 取自 principal 不在
    body、transport 服务端固定 FETCH_SSE、generation foreign/absent 经
    ensure_realtime_stream raise → DataAccessException 映射 404
    不披露、BadSqlGrammarException 上抛保 503、parseId/parseNonNegative 校验→400）+
    AuthDataSourceConfig wire 3 个 realtime
    bean（RealtimeTicketRepository/RealtimeResumeService/RealtimeEventRepository，resume/event
    为 SSE 卡铺路）+ RealtimeTicketControllerTest 9 场景（happy/404/503 上抛/非法 id/零
    streamEpoch/负 afterSeq/缺 sessionId/缺 origin）；run-rls 69/69 + mvn runtime
    320/0/0（persistence 102/0/0，+9 controller test）+ openapi diff PASS + diff
    exit0 全绿。下一步：realtime SSE resume 端点（SseEmitter + V8 resume_stream + 契约补
    resume endpoint，C3 contract-change）按序；ID hashid 编码随后。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
