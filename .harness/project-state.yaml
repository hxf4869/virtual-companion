schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0144
activeTaskCard: docs/tasks/TASK-0144-p2-12-jdbc-authorization-snapshot-one-way.md
lastAcceptedTask: TASK-0143
lastAcceptedHandoff: docs/handoffs/TASK-0143.json
lastTerminalTask: TASK-0143
lastTerminalHandoff: docs/handoffs/TASK-0143.json
nextAction: '执行 TASK-0144（P2-12 JDBC 授权快照单向生命周期）：JdbcAuthorizationSnapshotStore.put 改 insert-only（ON CONFLICT DO NOTHING + 重复即异常），withdraw/narrow 改带状态条件的单向转换（仅 ACTIVE 可转，行锁由条件 UPDATE 保证）并补 narrow ID 一致性校验；新增 JdbcAuthorizationSnapshotStoreTest（Mockito 断言 SQL/异常语义）与 infra/db/tests/51 真实 PostgreSQL 单向生命周期测试；冻结 clean candidate 后取得 15 分钟内独立 C3 Reviewer 终态，再依序完成唯一 Precheck、根级 Docker Maven verify、完整 SQL suite、唯一 diff check、Evidence/Handoff、单父终态提交和远端 0/0；随后处理 P3-01、P3-02/P3-07 独立卡、Provider DNS、数据库 Owner gates 与条件风险（P2-27/P2-25 待 Owner 决策），最后对最新 origin/main 做独立全项目复审。'
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-11'
