schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0024
activeTaskCard: docs/tasks/TASK-0024-relationship-active-companion.md
lastAcceptedTask: TASK-0023
lastAcceptedHandoff: docs/handoffs/TASK-0023.json
lastTerminalTask: TASK-0023
lastTerminalHandoff: docs/handoffs/TASK-0023.json
nextAction: TASK-0024（Relationship 与唯一活跃 Companion）已 READY 并授权。实现 V9 迁移（vc.relationship 上 partial unique index (owner_user_id) WHERE active 作为 activeCompanionLimit=1 唯一权威不变量 + 5 个 SECURITY DEFINER 生命周期函数 create/get/list/activate/deactivate，advisory lock 序列化同 owner、revoke PUBLIC 仅 grant vc_api、FORCE RLS）与 4 个 SQL 测试；OpenAPI 新增 Relationship 端点复用既有 ErrorEnvelope/ErrorCode 不新增错误码；验证 run-rls-tests + openapi validate/drift + harness unittest 全绿后独立 R1、终态 closure、ACCEPTED。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-06'
