schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: null
activeTaskCard: null
lastAcceptedTask: TASK-0184
lastAcceptedHandoff: docs/handoffs/TASK-0184.json
lastTerminalTask: TASK-0184
lastTerminalHandoff: docs/handoffs/TASK-0184.json
nextAction: >-
    TASK-0184（realtime SSE resume 端点）闭环 ACCEPTED：realtime-contract.yaml
    resume 段补 endpoint=GET /api/v1/realtime/streams/{generationId} +
    ticketBinding/ticketBoundTo/deniedEvent；OpenAPI 加该 SSE
    端点（text/event-stream）+ RealtimeStreamEvent schema + generate 重建
    dist；RealtimeStreamController（SseEmitter + consume ticket + resume 5
    disposition 映射逐个 id=eventSeq；ticket 失败/越权 fail closed stream.denied
    不披露；BadSqlGrammar re-throw 503；建连前参数错 400）+ pom 加 jackson-databind
    compile + 12 项 asyncDispatch 测试；runtime mvn 332/0/0 + openapi diff
    PASS + diff exit0。下一步：H5 离线聊天/流式显示与断线恢复（Fetch-SSE 客户端，开卡时分配下一永久
    ID）按序；policy 测试卡住独立排查随后。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-13'
