schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0173
activeTaskCard: docs/tasks/TASK-0173-worker-claim-coordinator-scheduled-polling.md
lastAcceptedTask: TASK-0172
lastAcceptedHandoff: docs/handoffs/TASK-0172.json
lastTerminalTask: TASK-0172
lastTerminalHandoff: docs/handoffs/TASK-0172.json
nextAction: TASK-0173（§5.1.2 worker claim lease/fence coordinator，C4，@Scheduled 轮询）实现中：V24
  list_pending_owner_ids/recover_expired_claims 授 vc_api + test 64 双会话（跨连接/过期回收/接管/旧 fence
  零写入）+ runtime WorkItemCoordinator（recover → 枚举 → 每 owner 新 fence + processOwnerBatch）+
  AuthDataSourceConfig bean + WorkItemCoordinatorTest + SchemaReadiness 23→24；重试/死信范围外。P2-22 已
  闭环。下一步：本卡 ACCEPTED 后继续逐项解锁剩余审计项（P2-20 验收卡、P2-26/P3-01 等按序开卡）；全部修复
  后对最新 origin/main 做独立全项目复审（串行完整 Harness unittest discover + canonical precheck + 根级
  Maven verify，作为 longline 收尾总 PASS）。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-12'
