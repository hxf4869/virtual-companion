schemaVersion: 1
projectId: virtual-companion
phase: TECHNICAL_ALPHA
phaseSource: specs/catalog/product-scope.yaml
activeTask: TASK-0099
activeTaskCard: docs/tasks/TASK-0099-refresh-rotation-single-successor.md
lastAcceptedTask: TASK-0098
lastAcceptedHandoff: docs/handoffs/TASK-0098.json
lastTerminalTask: TASK-0098
lastTerminalHandoff: docs/handoffs/TASK-0098.json
nextAction: TASK-0099 已授权（READY）：实施 P1-06 Refresh rotation 修复——V14 rotate 行锁/条件 UPDATE RETURNING 唯一 winner（revoked_at IS NULL AND expires_at > now()）、AuthService.refresh() 返回传给 rotate 的同一明文 token（不再调用 issueTokens() 创建第二 session）、并发 refresh 单 live successor 的 DB+服务层测试（48 并发测试 + AuthServiceTest）。
capabilityGates:
  businessImplementation:
    state: BLOCKED
    reason: TASK-0002 仅授权治理变更；完成后仍需新的 READY 业务任务
  realUserBeta:
    state: BLOCKED
    reason: PIA、伦理适用性、成年验证、责任人、值班和安全演练尚未全部形成证据
  realPayment:
    state: FORBIDDEN
    reason: Technical Alpha 范围明确禁止真实支付
updatedAt: '2026-08-08'
