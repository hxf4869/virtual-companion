harness-precheck:
  image: python:3.12-slim
  stage: test
  before_script:
    - python -m pip install --no-cache-dir -r requirements-harness.txt
  script:
    - bash scripts/harness/precheck.sh
  artifacts:
    when: always
    paths:
      - specs/generated/
