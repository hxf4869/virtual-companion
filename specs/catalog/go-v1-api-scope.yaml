schemaVersion: 1
catalogId: go-v1-api-scope
adr: docs/decisions/0007-go-companion-runtime.md
sourceOpenApi: specs/openapi/virtual-companion.yaml
runtimeAfterCutover: Go companiond serves the KEEP/SIMPLIFY surface after the 2026-08-30 cutover;
  DEFER/RETIRE operations are not runtime fallbacks.
note: 'The API consumer matrix remains the deletion/scope source. KEEP requires core-outcome, current H5 production caller, safety/data-rights,
  or Owner instruction. DEFER/RETIRE with H5 callers must hide or delete that entry in the same later
  slice that removes the OpenAPI operation. This file is the OpenAPI deletion list: RETIRE operations
  are removed with H5 in a later slice, not in G0.'
counts:
  KEEP: 62
  SIMPLIFY: 6
  DEFER: 15
  RETIRE: 39
operations:
- operationId: getVersion
  method: GET
  path: /api/v1/version
  currentH5Callers:
  - frontend/src/api/admin-runtime.ts
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep public version identity. Liveness/readiness stay outside this OpenAPI surface
    (actuator/health).
- operationId: getServiceMode
  method: GET
  path: /api/v1/service-mode
  currentH5Callers:
  - frontend/src/api/admin-runtime.ts
  decision: KEEP
  keepBecause:
  - h5-caller
  replacementOrImpact: Keep generation availability in the H5 administration console. Consumer pages
    use friendly availability states and do not expose service-mode terminology.
- operationId: getAgeState
  method: GET
  path: /api/v1/age/state
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep simulated adult state for the unique Owner. Remote age provider remains DEFER.
- operationId: verifyAge
  method: POST
  path: /api/v1/age/verification
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep Owner simulated adult verification only. No documents or biometrics.
- operationId: submitAgeAppeal
  method: POST
  path: /api/v1/age/appeal
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Remote age appeal is DEFER and has no current frontend entry.
- operationId: listAgeAppeals
  method: GET
  path: /api/v1/age/appeals
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Appeal history is DEFER and has no current frontend entry. Simulated adult state
    remains on getAgeState.
- operationId: getUsageHealth
  method: GET
  path: /api/v1/usage-health
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Usage-health is DEFER and has no current frontend entry.
- operationId: updateUsageHealthPrefs
  method: PUT
  path: /api/v1/usage-health
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Usage-health preferences are DEFER and have no current frontend entry.
- operationId: usageHealthHeartbeat
  method: POST
  path: /api/v1/usage-health/heartbeat
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: The current chat send/stream does not depend on a usage-health heartbeat.
- operationId: recordUsageReminder
  method: POST
  path: /api/v1/usage-health/reminder
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Usage reminders are DEFER and have no current frontend entry.
- operationId: getIncognitoPref
  method: GET
  path: /api/v1/incognito-pref
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep the server-side incognito default. The current frontend has no settings entry.
- operationId: updateIncognitoPref
  method: PUT
  path: /api/v1/incognito-pref
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep the server capability without exposing a current frontend entry.
- operationId: createRelationship
  method: POST
  path: /api/v1/relationships
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: The server creates the unique default companion during first authenticator setup;
    the current frontend does not expose manual creation.
- operationId: listRelationships
  method: GET
  path: /api/v1/relationships
  currentH5Callers:
  - frontend/src/stores/relationship.ts
  decision: KEEP
  keepBecause:
  - h5-caller
  replacementOrImpact: Keep list because Home, Chat and All Conversations resolve the server-created
    default relationship through the relationship store.
- operationId: getRelationship
  method: GET
  path: /api/v1/relationships/{relationshipId}
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: No production H5 caller. List plus prefs/update cover the unique companion. Delete
    in a later OpenAPI/H5 slice.
- operationId: activateRelationship
  method: POST
  path: /api/v1/relationships/{relationshipId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Retain the server lifecycle operation; the current frontend does not expose a switcher.
- operationId: deleteRelationship
  method: DELETE
  path: /api/v1/relationships/{relationshipId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Retain server-side relationship deletion without a current frontend entry.
- operationId: updateRelationshipPrefs
  method: PATCH
  path: /api/v1/relationships/{relationshipId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Retain server-side companion preferences without the retired settings page.
- operationId: deactivateRelationship
  method: POST
  path: /api/v1/relationships/{relationshipId}/deactivate
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Retain the server lifecycle operation; the current frontend keeps one active default.
- operationId: previewRelationshipClearance
  method: GET
  path: /api/v1/relationships/{relationshipId}/clearance-preview
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Keep clearance preview as a data-rights backend capability.
- operationId: resetRelationship
  method: POST
  path: /api/v1/relationships/{relationshipId}/reset
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Keep companion data reset as a data-rights backend capability.
- operationId: listMemoryImports
  method: GET
  path: /api/v1/memory-imports
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Memory import is DEFER and has no current frontend entry.
- operationId: discardMemoryImport
  method: DELETE
  path: /api/v1/memory-imports
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Discard import is DEFER and has no current frontend entry.
- operationId: importMemories
  method: POST
  path: /api/v1/relationships/{relationshipId}/memory-imports
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Import is DEFER and has no current frontend entry.
- operationId: createConversation
  method: POST
  path: /api/v1/conversations
  currentH5Callers:
  - frontend/src/stores/chat.ts
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  replacementOrImpact: Keep creating or continuing a conversation.
- operationId: listConversations
  method: GET
  path: /api/v1/conversations
  currentH5Callers:
  - frontend/src/stores/chat.ts
  - frontend/src/pages/index/index.vue
  - frontend/src/pages/conversations/conversations.vue
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  replacementOrImpact: Keep the Home / All Conversations summary source. Go v1 returns newest activity
    first, exposes lastActivityAt, and resolves the after conversation id to the same activity tuple for
    keyset pagination.
- operationId: getConversationSummary
  method: GET
  path: /api/v1/conversations/{conversationId}/summary
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: No H5 caller. Summary becomes an internal context capability; display it in conversation
    responses if needed later.
- operationId: previewChatWipe
  method: GET
  path: /api/v1/conversations/wipe-preview
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Keep wipe preview as a data-rights backend capability.
- operationId: wipeAllChats
  method: POST
  path: /api/v1/conversations/wipe
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Keep deleting all chats as a data-rights backend capability.
- operationId: deleteConversation
  method: DELETE
  path: /api/v1/conversations/{conversationId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Retain conversation deletion without exposing a current frontend management entry.
- operationId: renameConversation
  method: PATCH
  path: /api/v1/conversations/{conversationId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Retain server-side title management; the current conversations page is read-only.
- operationId: endConversation
  method: POST
  path: /api/v1/conversations/{conversationId}/end
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Retain the server conversation lifecycle without a current frontend entry.
- operationId: sendGeneration
  method: POST
  path: /api/v1/conversations/{conversationId}/generations
  currentH5Callers:
  - frontend/src/stores/chat.ts
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  replacementOrImpact: Keep send as StartTurn. One turn, one final assistant message; no version select.
- operationId: listMessages
  method: GET
  path: /api/v1/conversations/{conversationId}/messages
  currentH5Callers:
  - frontend/src/stores/chat.ts
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  replacementOrImpact: Keep message history.
- operationId: deleteMessage
  method: DELETE
  path: /api/v1/conversations/{conversationId}/messages/{messageId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Keep single-message deletion as a data-rights backend capability.
- operationId: setMessageNoMemory
  method: PATCH
  path: /api/v1/conversations/{conversationId}/messages/{messageId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep the server-side no-memory invariant. The current chat UI has no per-message switch.
- operationId: selectGenerationVersion
  method: POST
  path: /api/v1/generations/{generationId}/select
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: One turn has one final result; the current frontend has no version selector.
- operationId: listGenerationVersions
  method: GET
  path: /api/v1/messages/{messageId}/generation-versions
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The current frontend has no generation-version list.
- operationId: cancelGeneration
  method: POST
  path: /api/v1/generations/{generationId}/cancel
  currentH5Callers:
  - frontend/src/stores/chat.ts
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  replacementOrImpact: Keep cancel. SSE disconnect must not imply cancel.
- operationId: getGenerationSnapshot
  method: GET
  path: /api/v1/generations/{generationId}/snapshot
  currentH5Callers:
  - frontend/src/api/realtime-transport.ts
  - frontend/src/stores/chat.ts
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  replacementOrImpact: Keep durable snapshot for reconnect. Go v1 resume is snapshot-only.
- operationId: recordGenerationFeedback
  method: POST
  path: /api/v1/generations/{generationId}/feedback
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Retain feedback as a backend safety capability without a current chat entry.
- operationId: createRealtimeTicket
  method: POST
  path: /api/v1/realtime/tickets
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: SSE now uses the opaque session cookie; the H5 ticket helper has been removed.
- operationId: openRealtimeStream
  method: GET
  path: /api/v1/realtime/streams/{generationId}
  currentH5Callers:
  - frontend/src/api/realtime-transport.ts
  - frontend/src/stores/chat.ts
  decision: SIMPLIFY
  replacementOrImpact: Keep authenticated SSE. Drop ticket/gap/reset query binding; reconnect uses snapshot.
    Same path, new auth at Phase 5.
- operationId: createMemoryCandidate
  method: POST
  path: /api/v1/relationships/{relationshipId}/memories/candidates
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep explicit Owner candidate create. No auto-save and no MEMORY_EXTRACT job in
    Go v1.
- operationId: listMemories
  method: GET
  path: /api/v1/relationships/{relationshipId}/memories
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep listing canonical and pending memory.
- operationId: getMemory
  method: GET
  path: /api/v1/memories/{memoryId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep the memory detail backend capability without a current frontend entry.
- operationId: updateMemory
  method: PATCH
  path: /api/v1/memories/{memoryId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep Owner edit of canonical memory.
- operationId: deleteMemory
  method: DELETE
  path: /api/v1/memories/{memoryId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep delete/tombstone. Restore must not resurrect.
- operationId: confirmMemoryCandidate
  method: POST
  path: /api/v1/memories/{memoryId}/confirm
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep explicit confirm. Unconfirmed candidates are not Prompt facts.
- operationId: rejectMemoryCandidate
  method: POST
  path: /api/v1/memories/{memoryId}/reject
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep explicit reject.
- operationId: listMemoryEvidence
  method: GET
  path: /api/v1/memories/{memoryId}/evidence
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Keep evidence because server-side memory must remain attributable.
- operationId: getMemoryAutoSave
  method: GET
  path: /api/v1/memories/auto-save
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Go v1 has no auto-save and the current frontend has no memory switch.
- operationId: setMemoryAutoSave
  method: PUT
  path: /api/v1/memories/auto-save
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The current frontend has no auto-save toggle; candidates remain explicit only.
- operationId: createReminder
  method: POST
  path: /api/v1/relationships/{relationshipId}/reminders
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Reminder and proactive follow-up are DEFER with no current frontend entry.
- operationId: listReminders
  method: GET
  path: /api/v1/relationships/{relationshipId}/reminders
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: The redesigned Home does not expose reminders.
- operationId: updateReminder
  method: PATCH
  path: /api/v1/reminders/{reminderId}
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Reminder editing is DEFER with no current frontend entry.
- operationId: deleteReminder
  method: DELETE
  path: /api/v1/reminders/{reminderId}
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Reminder deletion is DEFER with no current frontend entry.
- operationId: createReport
  method: POST
  path: /api/v1/reports
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Keep safety report creation as a backend capability without a current frontend entry.
- operationId: listReports
  method: GET
  path: /api/v1/reports
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - safety-or-data-rights
  replacementOrImpact: Keep report history as a safety backend capability; no current frontend entry exists.
- operationId: getReport
  method: GET
  path: /api/v1/reports/{reportId}
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: No production H5 caller. List/create cover the Owner report result.
- operationId: assignServiceClass
  method: POST
  path: /api/v1/auth/admin/service-class
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Service class is RETIRE and is absent from the redesigned admin console.
- operationId: listServiceClassAssignments
  method: GET
  path: /api/v1/auth/admin/service-classes
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no service-class list.
- operationId: recordConsent
  method: PUT
  path: /api/v1/consents
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep record/withdraw. Outbound uses current consent, not a stale snapshot.
- operationId: listConsents
  method: GET
  path: /api/v1/consents
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep listing consent state.
- operationId: createExport
  method: POST
  path: /api/v1/exports
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  - safety-or-data-rights
  replacementOrImpact: Keep export create. MinIO/object lifecycle stays until a later dedicated slice.
- operationId: getExportStatus
  method: GET
  path: /api/v1/exports/{exportId}
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  - safety-or-data-rights
  replacementOrImpact: Keep export status.
- operationId: downloadExport
  method: GET
  path: /api/v1/exports/{exportId}/download
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - core-outcome
  - safety-or-data-rights
  replacementOrImpact: Keep one-time application download. No reusable presigned URL.
- operationId: login
  method: POST
  path: /api/v1/auth/login
  currentH5Callers:
  - frontend/src/stores/auth.ts
  - frontend/src/pages/login/login.vue
  decision: SIMPLIFY
  replacementOrImpact: Keep account/password as step one; account accepts username or email. It returns
    a five-minute TOTP/setup challenge, or issues opaque vc_session only when a valid trusted-device cookie
    skips TOTP. No JWT login.
- operationId: getRegistrationStatus
  method: GET
  path: /api/v1/auth/registration-status
  currentH5Callers:
  - frontend/src/stores/auth.ts
  - frontend/src/pages/login/login.vue
  decision: KEEP
  keepBecause:
  - owner-instruction
  replacementOrImpact: Server owns the registration switch. Launch value is false and the frontend must
    not invent a registration entry.
- operationId: register
  method: POST
  path: /api/v1/auth/register
  currentH5Callers: []
  decision: KEEP
  keepBecause:
  - owner-instruction
  replacementOrImpact: Keep a fail-closed launch endpoint. It accepts no account while email delivery is
    unconfigured and registration remains closed.
- operationId: getAuthSession
  method: GET
  path: /api/v1/auth/session
  currentH5Callers:
  - frontend/src/stores/auth.ts
  decision: KEEP
  keepBecause:
  - core-outcome
  - owner-instruction
  replacementOrImpact: Page refresh restores ACTIVE account state from the opaque cookie; H5 must not infer
    admission from local storage.
- operationId: getAuthenticatorSetup
  method: POST
  path: /api/v1/auth/challenges/{challengeId}/authenticator-setup
  currentH5Callers:
  - frontend/src/stores/auth.ts
  - frontend/src/pages/login/login.vue
  decision: KEEP
  keepBecause:
  - owner-instruction
  replacementOrImpact: Return one stable encrypted pending TOTP setup for the live first-login challenge.
- operationId: confirmAuthenticator
  method: POST
  path: /api/v1/auth/challenges/{challengeId}/authenticator-confirm
  currentH5Callers:
  - frontend/src/stores/auth.ts
  - frontend/src/pages/login/login.vue
  decision: KEEP
  keepBecause:
  - core-outcome
  - owner-instruction
  replacementOrImpact: First valid TOTP atomically enables the factor, returns ten one-time recovery codes,
    creates the default relationship, and issues the session.
- operationId: verifyAuthenticatorCode
  method: POST
  path: /api/v1/auth/challenges/{challengeId}/totp
  currentH5Callers:
  - frontend/src/stores/auth.ts
  - frontend/src/pages/login/login.vue
  decision: KEEP
  keepBecause:
  - core-outcome
  - owner-instruction
  replacementOrImpact: Daily second factor. An optional default-off choice issues a fixed 90-day trusted device.
- operationId: verifyRecoveryCode
  method: POST
  path: /api/v1/auth/challenges/{challengeId}/recovery-code
  currentH5Callers:
  - frontend/src/stores/auth.ts
  - frontend/src/pages/login/login.vue
  decision: KEEP
  keepBecause:
  - core-outcome
  - owner-instruction
  replacementOrImpact: Keep one-time recovery login; the code is consumed atomically and plaintext is never stored.
- operationId: refresh
  method: POST
  path: /api/v1/auth/refresh
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Refresh rotation is retired. H5 restores the opaque cookie through getAuthSession.
- operationId: logout
  method: POST
  path: /api/v1/auth/logout
  currentH5Callers:
  - frontend/src/stores/auth.ts
  decision: SIMPLIFY
  replacementOrImpact: Revoke only current vc_session. Ordinary logout preserves the independent trusted-device
    TOTP bypass, while email and password remain mandatory next time.
- operationId: listAuthSessions
  method: GET
  path: /api/v1/auth/sessions
  currentH5Callers: []
  decision: SIMPLIFY
  replacementOrImpact: Retain the opaque-session backend operation; the current My page lists trusted devices only.
- operationId: revokeAuthSession
  method: DELETE
  path: /api/v1/auth/sessions/{sessionId}
  currentH5Callers: []
  decision: SIMPLIFY
  replacementOrImpact: Retain single-session revocation without a current frontend entry.
- operationId: revokeAllAuthSessions
  method: POST
  path: /api/v1/auth/sessions/revoke-all
  currentH5Callers: []
  decision: SIMPLIFY
  replacementOrImpact: Retain revoke-all as a backend security capability without a current frontend entry.
- operationId: listTrustedDevices
  method: GET
  path: /api/v1/auth/trusted-devices
  currentH5Callers:
  - frontend/src/pages/account/account.vue
  decision: KEEP
  keepBecause:
  - owner-instruction
  replacementOrImpact: Account security lists active device labels and timestamps without tokens or fingerprints.
- operationId: revokeTrustedDevice
  method: DELETE
  path: /api/v1/auth/trusted-devices/{deviceId}
  currentH5Callers:
  - frontend/src/pages/account/account.vue
  decision: KEEP
  keepBecause:
  - owner-instruction
  replacementOrImpact: Revoke one owner-scoped trusted-device TOTP bypass.
- operationId: changeAuthPassword
  method: POST
  path: /api/v1/auth/password
  currentH5Callers:
  - frontend/src/pages/account/account.vue
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  replacementOrImpact: Keep password change. Existing BCrypt hashes remain.
- operationId: reauthAuth
  method: POST
  path: /api/v1/auth/reauth
  currentH5Callers:
  - frontend/src/pages/admin/admin.vue
  - frontend/src/pages/admin-accounts/admin-accounts.vue
  - frontend/src/pages/admin-models/admin-models.vue
  - frontend/src/pages/admin-routing/admin-routing.vue
  decision: KEEP
  keepBecause:
  - core-outcome
  replacementOrImpact: Keep fresh reauth for high-risk operations. Admin reset is not the Go consumer;
    account delete/export already require current password.
- operationId: adminResetAuthenticator
  method: POST
  path: /api/v1/admin/accounts/{accountId}/authenticator-reset
  currentH5Callers:
  - frontend/src/pages/admin-accounts/admin-accounts.vue
  decision: KEEP
  keepBecause:
  - owner-instruction
  replacementOrImpact: A recently reauthenticated ADMIN clears TOTP and recovery codes and revokes the target's
    sessions, challenges and trusted devices so next login must set up again.
- operationId: listAdminAccounts
  method: GET
  path: /api/v1/admin/accounts
  currentH5Callers:
  - frontend/src/pages/admin/admin.vue
  - frontend/src/pages/admin-accounts/admin-accounts.vue
  decision: KEEP
  keepBecause:
  - h5-caller
  - owner-instruction
  replacementOrImpact: Secret-free account facts support the real registration-review and account-security pages.
- operationId: reviewAdminAccount
  method: POST
  path: /api/v1/admin/accounts/{accountId}/review
  currentH5Callers:
  - frontend/src/pages/admin/admin.vue
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  - owner-instruction
  replacementOrImpact: A freshly reauthenticated ADMIN approves or rejects an email-verified pending account.
- operationId: adminResetPassword
  method: POST
  path: /api/v1/auth/admin/accounts/{accountId}/reset-password
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console does not expose password reset. My uses changeAuthPassword.
- operationId: deleteAccount
  method: DELETE
  path: /api/v1/auth/account
  currentH5Callers:
  - frontend/src/pages/account/account.vue
  decision: KEEP
  keepBecause:
  - core-outcome
  - h5-caller
  - safety-or-data-rights
  replacementOrImpact: Keep self-service account deletion with current-password confirmation.
- operationId: createInternalAccount
  method: POST
  path: /api/v1/auth/admin/accounts
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Registration remains server-controlled; the redesigned admin console has no account creator.
- operationId: listInternalAccounts
  method: GET
  path: /api/v1/auth/admin/accounts
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Replaced by listAdminAccounts; E2E helpers are not a production H5 caller.
- operationId: disableInternalAccount
  method: POST
  path: /api/v1/auth/admin/accounts/{accountId}/disable
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no disable entry.
- operationId: listAdminAuditEvents
  method: GET
  path: /api/v1/auth/admin/audit
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no audit-event view.
- operationId: getAdminUsageSummary
  method: GET
  path: /api/v1/auth/admin/usage
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no usage summary. Go v1 keeps internal limits.
- operationId: getAdminProviderPlan
  method: GET
  path: /api/v1/auth/admin/provider-plan
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Provider-plan projection is absent from the redesigned console; no cost is fabricated.
- operationId: grantTrial
  method: POST
  path: /api/v1/auth/admin/trial-grants
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Trial and entitlement are RETIRE with no current frontend entry.
- operationId: quotaReconciliation
  method: GET
  path: /api/v1/auth/admin/quota-reconciliation
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Product quota reconciliation is RETIRE with no current frontend entry.
- operationId: providerRegistry
  method: GET
  path: /api/v1/auth/admin/provider-registry
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The retired deployment-registry projection is replaced with
    the administrator-owned provider/model configuration and global route order below.
- operationId: listModelProviders
  method: GET
  path: /api/v1/admin/providers
  currentH5Callers:
  - frontend/src/pages/admin-models/admin-models.vue
  - frontend/src/pages/admin-routing/admin-routing.vue
  - frontend/src/api/admin-runtime.ts
  decision: KEEP
  keepBecause:
  - h5-caller
  - owner-instruction
  replacementOrImpact: Keep the secret-free ADMIN configuration view for multiple providers and models.
- operationId: saveModelProvider
  method: PUT
  path: /api/v1/admin/providers/{providerId}
  currentH5Callers:
  - frontend/src/pages/admin-models/admin-models.vue
  decision: KEEP
  keepBecause:
  - h5-caller
  - owner-instruction
  replacementOrImpact: Keep atomic provider/model snapshot save; credentials are write-only and encrypted.
- operationId: discoverProviderModels
  method: POST
  path: /api/v1/admin/providers/{providerId}/models/discover
  currentH5Callers:
  - frontend/src/pages/admin-models/admin-models.vue
  decision: KEEP
  keepBecause:
  - h5-caller
  - owner-instruction
  replacementOrImpact: Keep explicit bounded model-catalog discovery; results remain unsaved draft candidates.
- operationId: reorderModelRoutes
  method: PUT
  path: /api/v1/admin/model-routing-order
  currentH5Callers:
  - frontend/src/pages/admin-routing/admin-routing.vue
  decision: KEEP
  keepBecause:
  - h5-caller
  - owner-instruction
  replacementOrImpact: Keep the complete deterministic global provider/model priority chain.
- operationId: getTrialStatus
  method: GET
  path: /api/v1/trial-status
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Hide trial status on the health page in the same slice.
- operationId: recordSurvey
  method: POST
  path: /api/v1/survey
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: Survey is DEFER and the redesigned My page has no score card.
- operationId: listMySurveys
  method: GET
  path: /api/v1/survey
  currentH5Callers: []
  decision: DEFER
  replacementOrImpact: No production caller. Do not port. Hide if an entry appears later.
- operationId: listSafetyEvents
  method: GET
  path: /api/v1/auth/admin/safety-events
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no safety-event queue.
- operationId: listOpsCases
  method: GET
  path: /api/v1/auth/admin/ops-cases
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Beta operator cases are RETIRE and absent from the redesigned console.
- operationId: transitionOpsCase
  method: POST
  path: /api/v1/auth/admin/ops-cases/{caseId}/actions
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no ops-case transitions.
- operationId: updateOpsCaseNote
  method: PUT
  path: /api/v1/auth/admin/ops-cases/{caseId}/notes
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no ops-case notes.
- operationId: readOpsCaseInternalNote
  method: GET
  path: /api/v1/auth/admin/ops-cases/{caseId}/internal-note
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no internal-note view.
- operationId: setRetentionLegalHold
  method: POST
  path: /api/v1/auth/admin/retention-holds
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Retention legal-hold UI is RETIRE. 30-day dogfood retention and tombstones remain.
- operationId: releaseRetentionLegalHold
  method: DELETE
  path: /api/v1/auth/admin/retention-holds/{holdId}
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: RETIRE with setRetentionLegalHold.
- operationId: listBetaReports
  method: GET
  path: /api/v1/auth/admin/reports
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no Beta report queue.
- operationId: listBetaAgeAppeals
  method: GET
  path: /api/v1/auth/admin/age-appeals
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no age-appeal queue. Remote appeal remains DEFER.
- operationId: resolveAgeAppeal
  method: POST
  path: /api/v1/auth/admin/age-appeals/{appealId}/resolve
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no appeal-resolution entry.
- operationId: listBetaExportTasks
  method: GET
  path: /api/v1/auth/admin/export-tasks
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no Beta export-task queue.
- operationId: listBetaMemorySampling
  method: GET
  path: /api/v1/auth/admin/memory-sampling
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no memory-sampling view.
- operationId: getEmergencyContact
  method: GET
  path: /api/v1/emergency-contact
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Emergency contact stays closed with no current frontend entry.
- operationId: saveEmergencyContact
  method: PUT
  path: /api/v1/emergency-contact
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Emergency-contact save has no current frontend entry.
- operationId: startEmergencyContactVerification
  method: POST
  path: /api/v1/emergency-contact/verify-start
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Contact verification has no current frontend entry.
- operationId: confirmEmergencyContactVerification
  method: POST
  path: /api/v1/emergency-contact/verify-confirm
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Contact verification has no current frontend entry.
- operationId: revokeEmergencyContact
  method: POST
  path: /api/v1/emergency-contact/revoke
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Contact revoke has no current frontend entry. Crisis copy may show real-world resources without claiming
    notification.
undeclaredLegacyOperations:
- path: /api/v1/auth/admin/invites
  method: POST
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Invite mint is not in OpenAPI and has no current admin entry.
- path: /api/v1/auth/admin/invites
  method: GET
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no invite list.
- path: /api/v1/auth/admin/invites/disable
  method: POST
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: The redesigned admin console has no invite-disable action.
- path: /api/v1/auth/invite-register
  method: POST
  currentH5Callers: []
  decision: RETIRE
  replacementOrImpact: Public and invite registration stay closed with no current login entry.
